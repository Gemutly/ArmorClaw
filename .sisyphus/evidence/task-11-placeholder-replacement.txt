Task 11: Replace ArmorChat Placeholder Integration Tests
==========================================================

## File Modified
applications/ArmorChat/app/src/test/java/app/armorclaw/integration/IntegrationTest.kt

## Summary
Replaced all 12 `assertTrue("Integration test placeholder", true)` calls with either:
- Real assertions (2 SecurityConfig-related tests)
- @Ignore annotations with descriptive reasons (10 infrastructure-dependent tests)

## Verification Results

### Placeholder count (must be 0)
```
$ grep "assertTrue.*true" IntegrationTest.kt | grep -v "skip\|voice\|not.yet" | wc -l
0
```
Confirmed: ZERO placeholder assertions remain.

### Test count (must be 12)
```
$ grep "@Test" IntegrationTest.kt | wc -l
12
```
Confirmed: All 12 original tests preserved.

### Active vs Ignored
- 2 active tests with real SecurityConfig assertions
- 10 tests with @Ignore annotations

## Tests Replaced with Real Assertions

### 1. `complete setup flow from bonding to operational`
Tests SecurityConfigViewModel state management:
- Initial uiState is Loading
- Initial categories and permissions are empty
- setPermission/getPermission round-trip
- Unknown category defaults to "deny"
- configuredCount excludes "deny" entries
- resetToDefaults clears permissions map

### 2. `setup recovery after interruption`
Tests SecurityConfigViewModel state resilience:
- Permissions survive across operations
- clearError on non-error state is a no-op
- Permission overrides work correctly
- configuredCount updates accurately
- isSaving starts false, currentError starts null

## Tests Annotated with @Ignore (reason)

| Test | Ignore Reason |
|------|---------------|
| message send and receive flow | Requires real Matrix server |
| offline message queue and sync | Requires network simulation |
| network handover WiFi to Cellular | Requires network simulation |
| E2EE encrypt decrypt round trip | Requires vodozemac native library |
| group session key distribution | Requires vodozemac native library |
| FCM token registration flow | Requires Firebase integration |
| notification display and deep link | Requires UI testing framework |
| database migration from v1 to v2 | Requires Room testing |
| concurrent message send operations | Requires concurrent testing setup |
| race condition on rapid network changes | Requires network simulation |

## Syntax Verification
- Bracket balance verified: { 17 } 17 ✓, ( 76 ) 76 ✓
- kotlinc parse: no syntax errors (only unresolved reference errors due to missing classpath)
- No Gradle wrapper available for full compilation

## Dependencies Verified
- SecurityConfigViewModel: loadCategories, setPermission, getPermission, resetToDefaults, configuredCount, clearError, uiState, isSaving, currentError
- BridgeApi.DataCategory: used for type-safe empty list comparison
- SecurityConfigUiState: Loading, Loaded, BridgeError sealed class
