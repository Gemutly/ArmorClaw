Task 10: E2E Test - Deep Link Cold-Start and Warm-Resume
Date: 2026-04-18

## Files Modified

### 1. IntegrationTest.kt (replaced placeholder)
Path: applications/ArmorChat/app/src/test/java/app/armorclaw/integration/IntegrationTest.kt
- Replaced `notification display and deep link` test (was assertTrue(true) placeholder)
- Now uses DeepLinkHandler.handle() with real URI and asserts Route.Room result
- assertTrue(true) count: 12 → 11

### 2. DeepLinkE2ETest.kt (NEW)
Path: applications/ArmorChat/app/src/test/java/app/armorclaw/navigation/DeepLinkE2ETest.kt
- 20 test methods, 0 placeholders
- No new dependencies added

## Test Coverage

### Cold-start (3 tests)
- `cold-start room deep link resolves to Room route` — URI → Route.Room with correct roomId
- `cold-start email approval deep link resolves to EmailApproval route` — URI → Route.EmailApproval
- `cold-start with null URI data produces no route` — Uri.EMPTY → null

### Warm-resume (3 tests)
- `warm-resume room deep link resolves to Room route` — same handler, different roomId
- `warm-resume email approval deep link resolves to EmailApproval route`
- `warm-resume with unrecognised host produces no route` — unknown host → null

### Mid-setup queuing (8 tests)
- 6 tests: each setup route (Bonding, SecurityConfig, HardeningPassword, HardeningDevice, HardeningBiometrics, KeyBackup) is verified in SETUP_ROUTES
- 1 test: Home is NOT in setup routes → immediate navigation
- 1 test: combined — DeepLinkHandler resolves URI AND setup route membership check

### Multiple pending deep links (3 tests)
- `second deep link replaces first pending route` — verifies distinct route objects
- `different deep link types can both resolve independently` — Room + EmailApproval
- `queued deep link is consumed when leaving setup route` — Home not in setup routes

### Config deep link exclusion (2 tests + IntegrationTest)
- Config URI returns null in both cold-start and warm-resume scenarios

## Verification
- assertTrue(true) count in IntegrationTest.kt: 11 (down from 12)
- assertTrue(true) count in DeepLinkE2ETest.kt: 0
- No production code modified
- No new dependencies added
- Kotlin LSP unavailable; syntax verified via manual review
