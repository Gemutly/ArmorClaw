# v1.2 Deployment & Polish

## TL;DR

> **Quick Summary**: Wire the Artifact Envelope Protocol into the bridge RPC server, deploy v1.2 to the VPS, complete 4 high-value polish items (approval delegation, dynamic sidecar spawning, email team routing, dynamic PII config) that are 70-85% already built, run full regression, and produce a v1.3 roadmap for Browser/Email/Office infrastructure.
> 
> **Deliverables**:
> - `secretary.artifact_*` RPC handlers wired into bridge server
> - v1.2 Docker image deployed and validated on VPS
> - Approval delegation API completed
> - Dynamic sidecar spawning completed
> - Email team-based routing completed
> - Dynamic PII class configuration completed
> - Full regression battery passing
> - v1.3 roadmap document
> 
> **Estimated Effort**: Medium
> **Parallel Execution**: YES — 4 waves
> **Critical Path**: T1 (RPC wiring) → T2 (deploy) → T3-T6 (polish, parallel) → T7 (regression) → T8 (roadmap)

---

## Context

### Original Request
Deploy v1.2 Mobile Secretary to VPS, implement 5 high-value v1.2.1 polish items, stabilize deployment, prepare for Browser + Email phase.

### Analysis Findings (Pre-Plan Research)
**Key Discussions**:
- Docker image auto-builds on push to `main` via `dockerhub.yml` — no manual rebuild needed
- CRITICAL GAP: `secretary.artifact_*` handlers exist in `artifact_rpc.go` but are NOT registered in `bridge/pkg/rpc/server.go` — must wire before deployment
- 5 proposed polish items analyzed against codebase — 4 are genuine polish (70-85% built), 1 (Risk Thresholds) is architectural restructure (10% built)
- Risk Thresholds DROPPED to v1.3 — the classifier uses hardcoded taxonomy that needs full restructure

**Codebase Findings**:
- `DelegateTo` field already in `types.go` + `schema.sql` + `approvals.go:218` — delegation logic exists
- `SpawnToolSidecar()` in `toolsidecar.go` — full Docker lifecycle implemented
- `TeamMatcher` + `TeamAgentLookup` + `TeamRoutedEmailEvent` — email routing 85% built
- `CategoryConfig` struct with runtime config — PII configuration 50% built
- `RiskClassifierImpl` is hardcoded map — cannot be made configurable without restructure

### Metis Review (Self-Performed)
**Identified Gaps** (addressed):
- Gap: Artifact RPC handlers not wired → Added T1 as prerequisite
- Gap: No guarantee dockerhub.yml built ff2c613 → T2 includes build verification
- Gap: Risk thresholds would consume entire plan → Dropped to v1.3
- Gap: Artifact store needs SQLite path in production → T1 includes store initialization
- Guardrail: Do not touch cmd/ directory's main.go flow — only add to setup files
- Guardrail: Do not restructure risk_classifier.go — out of scope

---

## Work Objectives

### Core Objective
Deploy v1.2 to production and complete 4 polish items that are 70-85% already built in the codebase, then stabilize and plan the next phase.

### Concrete Deliverables
- `bridge/pkg/rpc/server.go` — 4 new `secretary.artifact_*` method registrations
- `bridge/cmd/bridge/setup_secretary.go` — ArtifactStore + ArtifactRPCHandler initialization
- VPS running v1.2 with artifact endpoints live
- 4 completed polish features with TDD tests
- Updated baseline report
- `docs/reference/v1.3-roadmap.md`

### Definition of Done
- [ ] `secretary.artifact_upload` responds on VPS without error
- [ ] `secretary.artifact_download` responds on VPS without error
- [ ] `secretary.artifact_list` responds on VPS without error
- [ ] `secretary.artifact_update_status` responds on VPS without error
- [ ] Approval delegation RPC endpoint works end-to-end
- [ ] Dynamic sidecar spawning with skill-based routing works
- [ ] Email team-based routing dispatches to correct agents
- [ ] PII configuration can be updated at runtime via RPC
- [ ] VPS regression battery: 0 new regressions
- [ ] v1.3 roadmap document exists

### Must Have
- Artifact RPC handlers wired into bridge server
- v1.2 deployed and validated on VPS
- Approval delegation completion
- Dynamic sidecar spawning completion
- Email team routing completion
- Dynamic PII config completion
- Full regression passing
- v1.3 roadmap

### Must NOT Have (Guardrails)
- NO restructuring of `risk_classifier.go` — out of scope (v1.3)
- NO Browser/Email/Office infrastructure deployment — v1.3
- NO Voice implementation — v1.4+
- NO changes to `cmd/bridge/main.go` startup flow — add to setup files only
- NO changes to `container-setup.sh` or `deploy/container-setup.sh`
- NO removal of SQLCipher, Matrix control plane, or approval flow
- NO new Docker images for sidecars — only the bridge image
- NO changes to existing test assertions — only additions
- NO structured logging library — stdlib `log` only
- NO ArmorChat Android code — bridge + API only

### v1.3 Deferred Items
These are valid but architecturally too large for a polish plan:
- Risk threshold configuration (hardcoded classifier restructure)
- Browser (Jetski) sidecar deployment
- Email pipeline infrastructure (Postfix, SMTP)
- Office document pipeline (Rust + Python sidecars)
- Voice stack implementation

---

## Verification Strategy

> **ZERO HUMAN INTERVENTION** — ALL verification is agent-executed. No exceptions.

### Test Decision
- **Infrastructure exists**: YES (Go test framework + bash harness + CI)
- **Automated tests**: TDD for all new code
- **Framework**: Go testing + bash test scripts

### QA Policy
Every task includes agent-executed QA scenarios.
Evidence saved to `.sisyphus/evidence/task-{N}-{scenario-slug}.{ext}`.

---

## Execution Strategy

### Parallel Execution Waves

```
Wave 0 (Prerequisite — Wire artifact RPC before anything else):
└── Task 1: Wire secretary.artifact_* into bridge RPC server [quick]
  EXIT GATE: go test ./pkg/rpc/... shows 4 new artifact methods registered

Wave 1 (Deploy — Get v1.2 live):
├── Task 2: Build, deploy, and validate v1.2 on VPS [deep]
  EXIT GATE: artifact endpoints respond on VPS, bridge healthy

Wave 2 (Polish — 4 parallel tasks, all 70-85% pre-built):
├── Task 3: Complete approval delegation API [quick]
├── Task 4: Complete dynamic sidecar spawning [unspecified-high]
├── Task 5: Complete email team-based routing [quick]
└── Task 6: Complete dynamic PII class configuration [unspecified-high]

Wave 3 (Stabilize + Plan):
├── Task 7: Full regression battery + report [deep]
└── Task 8: v1.3 roadmap document [writing]

Wave FINAL (4 parallel reviews):
├── F1: Plan compliance audit (oracle)
├── F2: Code quality review (unspecified-high)
├── F3: Live VPS integration QA (unspecified-high)
└── F4: Scope fidelity check (deep)
-> Present results -> Get explicit user approval

Critical Path: T1 → T2 → T7 → F1-F4 → user approval
Max Concurrent: 4 (Wave 2)
```

### Dependency Matrix

| Task | Depends On | Blocks | Wave |
|------|-----------|--------|------|
| 1 | — | 2 | 0 |
| 2 | 1 | 7, 8 | 1 |
| 3 | 1 | 7 | 2 |
| 4 | 1 | 7 | 2 |
| 5 | 1 | 7 | 2 |
| 6 | 1 | 7 | 2 |
| 7 | 2, 3, 4, 5, 6 | 8 | 3 |
| 8 | 7 | F1-F4 | 3 |

### Agent Dispatch Summary

- **Wave 0**: 1 task — T1 → `quick`
- **Wave 1**: 1 task — T2 → `deep`
- **Wave 2**: 4 tasks — T3 → `quick`, T4 → `unspecified-high`, T5 → `quick`, T6 → `unspecified-high`
- **Wave 3**: 2 tasks — T7 → `deep`, T8 → `writing`
- **FINAL**: 4 tasks — F1 → `oracle`, F2 → `unspecified-high`, F3 → `unspecified-high`, F4 → `deep`

---

## TODOs

- [x] 1. Wire `secretary.artifact_*` RPC handlers into bridge server

  **What to do**:
  - Read `bridge/pkg/rpc/server.go` lines 1314-1326 to understand the secretary method registration pattern
  - Read `bridge/cmd/bridge/setup_secretary.go` to understand how secretary services are initialized
  - Add 4 new method registrations to `server.go` handler map:
    - `"secretary.artifact_upload": s.handleArtifactMethod`
    - `"secretary.artifact_download": s.handleArtifactMethod`
    - `"secretary.artifact_list": s.handleArtifactMethod`
    - `"secretary.artifact_update_status": s.handleArtifactMethod`
  - Add `handleArtifactMethod` dispatch function that delegates to `ArtifactRPCHandler.Handle()`
  - Add `ArtifactStore` initialization in `setup_secretary.go` (SQLite path: `/var/lib/armorclaw/artifacts.db`)
  - Add `ArtifactRPCHandler` creation and injection into server config
  - Write TDD test: `TestArtifactMethodRegistration` verifying all 4 methods appear in handler map
  - Write TDD test: `TestArtifactRPCDispatch` verifying requests route to correct handler methods

  **Must NOT do**:
  - Do NOT modify `cmd/bridge/main.go` — add to `setup_secretary.go` only
  - Do NOT change existing method registrations
  - Do NOT restructure the server.go dispatch pattern

  **Recommended Agent Profile**:
  - **Category**: `quick`
  - **Skills**: []
    - This is a well-defined wiring task with clear patterns to follow

  **Parallelization**:
  - **Can Run In Parallel**: NO — blocks everything
  - **Parallel Group**: Wave 0 (first task)
  - **Blocks**: Tasks 2-8
  - **Blocked By**: None

  **References**:
  - `bridge/pkg/rpc/server.go:1314-1326` — Secretary method registration pattern (exact format to copy)
  - `bridge/pkg/secretary/artifact_rpc.go:15-48` — ArtifactRPCHandler with Handle() dispatch
  - `bridge/pkg/secretary/artifact_store.go:30-60` — ArtifactStore constructor and SQLite path
  - `bridge/cmd/bridge/setup_secretary.go:1-70` — Secretary service initialization pattern
  - `bridge/pkg/rpc/secretary_handlers_test.go` — Test pattern for method registration tests

  **Acceptance Criteria**:
  - [ ] `cd bridge && go test ./pkg/rpc/ -run TestArtifactMethodRegistration -v` → PASS
  - [ ] `cd bridge && go test ./pkg/secretary/ -run TestArtifactRPC -v` → PASS (existing 42 tests)
  - [ ] `cd bridge && go build ./cmd/bridge` → compiles without errors
  - [ ] No changes to existing method registrations or main.go

  **QA Scenarios**:
  ```
  Scenario: All 4 artifact methods registered in handler map
    Tool: Bash
    Steps:
      1. `cd bridge && go test ./pkg/rpc/ -run TestArtifactMethodRegistration -v -count=1`
      2. Assert output shows 4 PASS for artifact method registration
    Expected Result: All 4 methods found in handler map
    Failure Indicators: "method not registered" in output
    Evidence: .sisyphus/evidence/task-1-artifact-registration.txt

  Scenario: Bridge compiles with artifact handlers wired
    Tool: Bash
    Steps:
      1. `cd bridge && CGO_ENABLED=1 go build -o /dev/null ./cmd/bridge`
    Expected Result: Exit code 0, no errors
    Failure Indicators: Compilation error, undefined reference
    Evidence: .sisyphus/evidence/task-1-bridge-compile.txt
  ```

  **Commit**: YES
  - Message: `feat(secretary): wire artifact RPC handlers into bridge server`
  - Files: `bridge/pkg/rpc/server.go`, `bridge/cmd/bridge/setup_secretary.go`, `bridge/pkg/rpc/artifact_handlers_test.go` (new)

- [x] 2. Build, deploy, and validate v1.2 on VPS

  **What to do**:
  - Verify the GitHub Actions dockerhub.yml workflow ran successfully for commit `ff2c613` or later (includes artifact wiring from T1)
  - If not built: `docker build -t mikegemut/armorclaw:latest -f Dockerfile.quickstart .`
  - If auto-built: verify image digest matches latest on Docker Hub
  - SSH to VPS: `ssh -i ~/.ssh/openclaw_win root@5.183.11.149`
  - Pull latest image: `docker pull mikegemut/armorclaw:latest`
  - Restart container: `docker restart armorclaw` (or `docker compose up -d` if compose-based)
  - Wait for health: `curl -sk https://localhost:8443/health` → `status: ok`
  - Test artifact endpoints:
    - `secretary.artifact_list` with empty params → should return empty list, not "method not found"
    - `secretary.artifact_upload` with minimal envelope → should validate and store
    - `secretary.artifact_download` with the uploaded ID → should return the envelope
  - Verify existing bridge functions still work: `status`, `matrix.status`, `studio.list_agents`
  - Run quick smoke test of 5 core scripts on VPS

  **Must NOT do**:
  - Do NOT change production config or secrets
  - Do NOT restart Matrix Conduit
  - Do NOT modify bridge config.toml

  **Recommended Agent Profile**:
  - **Category**: `deep`
  - **Skills**: []

  **Parallelization**:
  - **Can Run In Parallel**: NO (sequential, depends on T1)
  - **Parallel Group**: Wave 1
  - **Blocks**: Tasks 3-8
  - **Blocked By**: Task 1

  **References**:
  - VPS: `5.183.11.149`, SSH key `~/.ssh/openclaw_win`
  - `.github/workflows/dockerhub.yml` — Auto-build workflow
  - `Dockerfile.quickstart` — Build definition (Go 1.25, CGO)
  - `bridge/pkg/secretary/artifact_rpc.go` — RPC handlers being validated
  - `tests/reports/v1.2-completion-report.md` — Pre-deployment state

  **Acceptance Criteria**:
  - [ ] Bridge healthy at `https://localhost:8443/health`
  - [ ] `secretary.artifact_list` returns JSON (not "method not found")
  - [ ] `secretary.artifact_upload` → `secretary.artifact_download` round-trip works
  - [ ] `matrix.status` shows `logged_in: true`
  - [ ] `studio.list_agents` returns valid JSON

  **QA Scenarios**:
  ```
  Scenario: Artifact upload-download round-trip on VPS
    Tool: Bash (SSH)
    Preconditions: Bridge running with new image
    Steps:
      1. SSH to VPS
      2. Upload: echo '{"jsonrpc":"2.0","id":1,"method":"secretary.artifact_upload","params":{"envelope":{"version":"1.0","metadata":{"filename":"test.txt","mime_type":"text/plain","source":"test"},"owner_id":"test-user","workflow_id":"test-wf","checksum":"abc123"}}}' | curl -sk --unix-socket /run/armorclaw/bridge.sock -X POST -H 'Content-Type: application/json' -d @- https://localhost/rpc
      3. Assert response contains artifact_id
      4. List: echo '{"jsonrpc":"2.0","id":2,"method":"secretary.artifact_list","params":{"workflow_id":"test-wf"}}' | curl -sk --unix-socket /run/armorclaw/bridge.sock -X POST -H 'Content-Type: application/json' -d @- https://localhost/rpc
      5. Assert response contains the uploaded artifact
    Expected Result: Upload returns artifact_id, list returns the artifact
    Failure Indicators: "method not found", error response, empty list
    Evidence: .sisyphus/evidence/task-2-vps-artifact-roundtrip.txt

  Scenario: Existing bridge functions unaffected
    Tool: Bash (SSH)
    Steps:
      1. SSH to VPS
      2. `curl -sk https://localhost:8443/health` → status ok
      3. `curl -sk https://localhost:6167/_matrix/client/versions` → versions array
    Expected Result: All existing endpoints respond normally
    Failure Indicators: Connection refused, error responses
    Evidence: .sisyphus/evidence/task-2-vps-health.txt
  ```

  **Commit**: YES (if manual build needed)
  - Message: `chore(deploy): deploy v1.2 with artifact protocol to VPS`

- [x] 3. Complete approval delegation API

  **What to do**:
  - Read existing delegation infrastructure:
    - `bridge/pkg/secretary/types.go:240-244` — `AutoApprove`, `DelegateTo`, `Conditions` fields already defined
    - `bridge/pkg/secretary/schema.sql:58-59` — `auto_approve`, `delegate_to` columns in schema
    - `bridge/pkg/secretary/approvals.go:206-236` — Delegation logic in `evaluateForApproval` (lines 218-219 check `policy.DelegateTo`)
    - `bridge/pkg/secretary/trusted_workflows.go` — Full trust policy engine (1,065 lines) with CRUD, evaluation, revocation
  - Add RPC method `secretary.set_approval_delegation` to configure delegation rules
  - Add RPC method `secretary.get_approval_delegation` to query current delegation
  - Add RPC method `secretary.revoke_approval_delegation` to remove delegation
  - Wire delegation timeout escalation (if delegate doesn't respond within N minutes, escalate to original approver)
  - Write TDD tests for delegation chain, revocation, and timeout escalation

  **Must NOT do**:
  - Do NOT restructure the existing approval engine
  - Do NOT change the `ApprovalDecision` type or `ApprovalRequest` struct
  - Do NOT remove existing approval flow behavior

  **Recommended Agent Profile**:
  - **Category**: `quick`
    - Reason: ~70% built, following existing patterns
  - **Skills**: []

  **Parallelization**:
  - **Can Run In Parallel**: YES (with Tasks 4, 5, 6)
  - **Parallel Group**: Wave 2
  - **Blocks**: Task 7
  - **Blocked By**: Task 1

  **References**:
  - `bridge/pkg/secretary/types.go:240-244` — AutoApprove, DelegateTo fields already exist
  - `bridge/pkg/secretary/schema.sql:58-59` — Schema columns already exist
  - `bridge/pkg/secretary/approvals.go:206-236` — Delegation dispatch logic at line 218
  - `bridge/pkg/secretary/trusted_workflows.go:274-312` — TrustedWorkflowEngine with Evaluate, CreatePolicy, RevokePolicy
  - `bridge/pkg/rpc/server.go:1314-1326` — Method registration pattern
  - `docs/reference/api-contracts-v1.2.md` — API contract documentation to extend

  **Acceptance Criteria**:
  - [ ] `go test ./pkg/secretary/ -run TestApprovalDelegation -v` → PASS
  - [ ] Delegation chain works: user A delegates to B, B can approve on A's behalf
  - [ ] Revocation works: delegated approval is no longer valid after revoke
  - [ ] Timeout escalation works: if delegate doesn't respond, escalates to original approver

  **QA Scenarios**:
  ```
  Scenario: Delegation round-trip (set → approve → verify)
    Tool: Bash
    Steps:
      1. `cd bridge && go test ./pkg/secretary/ -run TestApprovalDelegation -v -count=1`
      2. Assert all delegation tests PASS
    Expected Result: Delegation set, approve via delegate, revoke all work
    Evidence: .sisyphus/evidence/task-3-delegation.txt

  Scenario: Delegation timeout escalation
    Tool: Bash
    Steps:
      1. `cd bridge && go test ./pkg/secretary/ -run TestDelegationEscalation -v -count=1`
      2. Assert escalation test PASS
    Expected Result: After timeout, approval reverts to original approver
    Evidence: .sisyphus/evidence/task-3-delegation-escalation.txt
  ```

  **Commit**: YES (groups with Tasks 4-6)
  - Message: `feat(secretary): complete approval delegation API`
  - Files: `bridge/pkg/secretary/delegation.go` (new), `bridge/pkg/secretary/delegation_test.go` (new), `bridge/pkg/rpc/server.go`

- [x] 4. Complete dynamic sidecar spawning

  **What to do**:
  - Read existing sidecar infrastructure:
    - `bridge/pkg/toolsidecar/toolsidecar.go` — `Provisioner.SpawnToolSidecar()` with full Docker lifecycle (create, start, stop, remove)
    - `Config` struct with `DockerClient`, `Image`, `NetworkMode`, `ResourceLimits`
    - `ToolSidecar` struct with container ID, status, metadata
  - Add skill-based routing: map skill names (e.g., "document_processing") to sidecar images
  - Add idle timeout management: sidecars auto-stop after N minutes of inactivity
  - Add resource quota enforcement: max concurrent sidecars per workflow
  - Add RPC method `sidecar.spawn` with skill parameter
  - Add RPC method `sidecar.list` to show running sidecars
  - Add RPC method `sidecar.stop` to gracefully shut down a sidecar
  - Write TDD tests for skill routing, idle timeout, and quota enforcement

  **Must NOT do**:
  - Do NOT modify `bridge/pkg/toolsidecar/toolsidecar.go` existing functions
  - Do NOT add new Docker images — use existing sidecar images only
  - Do NOT change container security profiles

  **Recommended Agent Profile**:
  - **Category**: `unspecified-high`
    - Reason: ~80% built but needs careful integration
  - **Skills**: []

  **Parallelization**:
  - **Can Run In Parallel**: YES (with Tasks 3, 5, 6)
  - **Parallel Group**: Wave 2
  - **Blocks**: Task 7
  - **Blocked By**: Task 1

  **References**:
  - `bridge/pkg/toolsidecar/toolsidecar.go:57-101` — SpawnToolSidecar with Docker create/start/remove
  - `bridge/pkg/toolsidecar/toolsidecar.go:14-26` — DockerClient interface (mock-friendly)
  - `bridge/pkg/toolsidecar/toolsidecar.go:30-44` — Config and Provisioner constructor
  - `docs/reference/api-contracts-v1.2.md` — Sidecar contracts section

  **Acceptance Criteria**:
  - [ ] `go test ./pkg/toolsidecar/ -run TestSkillRouting -v` → PASS
  - [ ] `go test ./pkg/toolsidecar/ -run TestIdleTimeout -v` → PASS
  - [ ] `go test ./pkg/toolsidecar/ -run TestQuotaEnforcement -v` → PASS
  - [ ] Skill-to-image mapping configurable

  **QA Scenarios**:
  ```
  Scenario: Skill-based spawning routes to correct image
    Tool: Bash
    Steps:
      1. `cd bridge && go test ./pkg/toolsidecar/ -run TestSkillRouting -v -count=1`
      2. Assert "document_processing" skill maps to correct sidecar image
    Expected Result: Correct image selected per skill
    Evidence: .sisyphus/evidence/task-4-skill-routing.txt

  Scenario: Idle timeout stops inactive sidecars
    Tool: Bash
    Steps:
      1. `cd bridge && go test ./pkg/toolsidecar/ -run TestIdleTimeout -v -count=1`
      2. Assert sidecar is stopped after idle period
    Expected Result: Sidecar container stopped and removed after timeout
    Evidence: .sisyphus/evidence/task-4-idle-timeout.txt
  ```

  **Commit**: YES (groups with Tasks 3, 5, 6)
  - Message: `feat(toolsidecar): add skill-based dynamic sidecar spawning`
  - Files: `bridge/pkg/toolsidecar/skill_router.go` (new), `bridge/pkg/toolsidecar/skill_router_test.go` (new)

- [x] 5. Complete email team-based routing

  **What to do**:
  - Read existing email routing infrastructure:
    - `bridge/pkg/email/dispatcher.go:96-100` — `TeamMatcher` callback already called, `TeamRoutedEmailEvent` already emitted
    - `bridge/pkg/email/team_routing_integration_test.go` — 254-line integration test already validates routing
    - `bridge/pkg/email/events.go:44` — `TeamRoutedEmailEvent` type already defined
    - `bridge/pkg/email/types.go` — Email types with routing support
  - The routing LOGIC is complete. What's missing:
    - RPC method `email.set_team_routing` to configure routing rules
    - RPC method `email.get_team_routing` to query current rules
    - Persistent storage of routing rules (SQLite, following existing store pattern)
    - Pattern-based matching (sender domain, subject keywords) beyond the current callback approach
  - Write TDD tests for rule persistence, pattern matching, and rule priority

  **Must NOT do**:
  - Do NOT modify the existing `EmailDispatcher` core dispatch logic
  - Do NOT add Postfix or SMTP infrastructure
  - Do NOT change the `TeamMatcher` interface signature

  **Recommended Agent Profile**:
  - **Category**: `quick`
    - Reason: ~85% built, just needs persistence layer and RPC endpoints
  - **Skills**: []

  **Parallelization**:
  - **Can Run In Parallel**: YES (with Tasks 3, 4, 6)
  - **Parallel Group**: Wave 2
  - **Blocks**: Task 7
  - **Blocked By**: Task 1

  **References**:
  - `bridge/pkg/email/dispatcher.go:90-100` — Team routing dispatch (already calls matcher)
  - `bridge/pkg/email/team_routing_integration_test.go` — Existing integration test (copy pattern)
  - `bridge/pkg/email/events.go:44` — TeamRoutedEmailEvent type
  - `bridge/pkg/email/types.go` — Email type definitions
  - `bridge/pkg/secretary/store.go:170-185` — SQLite schema pattern for persistence

  **Acceptance Criteria**:
  - [ ] `go test ./pkg/email/ -run TestTeamRouting -v` → PASS
  - [ ] Routing rules persist across bridge restart
  - [ ] Pattern matching works for sender domain and subject keywords

  **QA Scenarios**:
  ```
  Scenario: Team routing rule persistence
    Tool: Bash
    Steps:
      1. `cd bridge && go test ./pkg/email/ -run TestTeamRoutingPersistence -v -count=1`
      2. Assert rules survive store close/reopen
    Expected Result: Rules loaded from SQLite after restart
    Evidence: .sisyphus/evidence/task-5-team-routing-persist.txt

  Scenario: Pattern-based email matching
    Tool: Bash
    Steps:
      1. `cd bridge && go test ./pkg/email/ -run TestTeamRoutingPatterns -v -count=1`
      2. Assert sender domain and subject patterns match correctly
    Expected Result: "sales@company.com" routes to sales team, subject "urgent" routes to ops team
    Evidence: .sisyphus/evidence/task-5-team-routing-patterns.txt
  ```

  **Commit**: YES (groups with Tasks 3, 4, 6)
  - Message: `feat(email): complete team-based routing with persistent rules`
  - Files: `bridge/pkg/email/routing_rules.go` (new), `bridge/pkg/email/routing_rules_test.go` (new)

- [x] 6. Complete dynamic PII class configuration

  **What to do**:
  - Read existing PII infrastructure:
    - `bridge/pkg/security/categories.go:15-34` — `DataCategory` constants (banking, pii, medical, residential, network, identity, location, credentials)
    - `bridge/pkg/security/categories.go:140` — `CategoryConfig` struct with `Permission`, `AllowedWebsites`, `BlockedWebsites`, `DataSubsets`, `RequiresApproval`, `AuditLevel`, `MaxRetention`
    - `bridge/pkg/security/categories.go` — `CategoryInfo` with regex patterns per category
  - Add RPC method `security.set_category_config` to update category configuration at runtime
  - Add RPC method `security.get_category_config` to query current configuration
  - Add runtime config reload (thread-safe via existing `sync.RWMutex` in `CategoryConfig`)
  - Add custom pattern support: allow adding new regex patterns per category
  - Add config persistence to SQLite (following `bridge/pkg/secretary/store.go` pattern)
  - Write TDD tests for runtime config update, custom patterns, persistence

  **Must NOT do**:
  - Do NOT remove or change existing `DataCategory` constants
  - Do NOT modify the default regex patterns
  - Do NOT change `CategoryConfig` field types

  **Recommended Agent Profile**:
  - **Category**: `unspecified-high`
    - Reason: ~50% built, needs careful thread-safe config management
  - **Skills**: []

  **Parallelization**:
  - **Can Run In Parallel**: YES (with Tasks 3, 4, 5)
  - **Parallel Group**: Wave 2
  - **Blocks**: Task 7
  - **Blocked By**: Task 1

  **References**:
  - `bridge/pkg/security/categories.go:15-34` — DataCategory enum (8 categories)
  - `bridge/pkg/security/categories.go:37-140` — CategoryInfo and CategoryConfig structs
  - `bridge/pkg/security/categories.go:140-170` — CategoryConfig with RWMutex, IsAllowed, audit methods
  - `bridge/pkg/secretary/store.go:170-185` — SQLite schema pattern for persistence
  - `docs/reference/api-contracts-v1.2.md` — Risk/trust contracts section

  **Acceptance Criteria**:
  - [ ] `go test ./pkg/security/ -run TestDynamicPIIConfig -v` → PASS
  - [ ] PII configuration can be updated at runtime via RPC
  - [ ] Custom regex patterns can be added per category
  - [ ] Configuration persists across bridge restart

  **QA Scenarios**:
  ```
  Scenario: Runtime PII config update
    Tool: Bash
    Steps:
      1. `cd bridge && go test ./pkg/security/ -run TestDynamicPIIConfig -v -count=1`
      2. Assert config update takes effect immediately
    Expected Result: Updated config visible in subsequent queries
    Evidence: .sisyphus/evidence/task-6-pii-config.txt

  Scenario: Custom pattern addition
    Tool: Bash
    Steps:
      1. `cd bridge && go test ./pkg/security/ -run TestCustomPIIPattern -v -count=1`
      2. Assert custom regex matches test data
    Expected Result: New pattern detects custom PII format
    Evidence: .sisyphus/evidence/task-6-custom-pattern.txt
  ```

  **Commit**: YES (groups with Tasks 3, 4, 5)
  - Message: `feat(security): add dynamic PII class configuration API`
  - Files: `bridge/pkg/security/config_api.go` (new), `bridge/pkg/security/config_api_test.go` (new), `bridge/pkg/security/config_store.go` (new)

- [x] 7. Full regression battery + report

  **What to do**:
  - SSH to VPS, run full 58-script battery
  - Compare against v1.2 baseline: 49 PASS / 9 MIXED / 0 INC
  - Document any new regressions with root cause
  - Test new v1.2.1 endpoints on VPS:
    - `secretary.set_approval_delegation` / `secretary.get_approval_delegation`
    - `sidecar.spawn` / `sidecar.list` / `sidecar.stop`
    - `email.set_team_routing` / `email.get_team_routing`
    - `security.set_category_config` / `security.get_category_config`
  - Write updated baseline report to `tests/reports/v1.2.1-regression-report.md`

  **Must NOT do**:
  - Do NOT change test scripts to fix failures — document only
  - Do NOT restart Matrix Conduit

  **Recommended Agent Profile**:
  - **Category**: `deep`
  - **Skills**: []

  **Parallelization**:
  - **Can Run In Parallel**: NO (sequential, after all polish tasks)
  - **Parallel Group**: Wave 3
  - **Blocks**: Task 8
  - **Blocked By**: Tasks 2, 3, 4, 5, 6

  **References**:
  - VPS: `5.183.11.149`, SSH key `~/.ssh/openclaw_win`
  - `tests/reports/v1.2-completion-report.md` — v1.2 baseline (49P/9MIXED/0INC)
  - `tests/reports/post-wave0-vps-battery.md` — Wave 0 baseline for comparison

  **Acceptance Criteria**:
  - [ ] Full 58-script battery runs to completion
  - [ ] 0 new regressions compared to v1.2 baseline
  - [ ] New v1.2.1 endpoints all respond (not "method not found")
  - [ ] Report written to `tests/reports/v1.2.1-regression-report.md`

  **QA Scenarios**:
  ```
  Scenario: Regression battery matches baseline
    Tool: Bash (SSH)
    Steps:
      1. SSH to VPS
      2. Run full 58-script battery with generous timeout
      3. Count PASS/MIXED/FAIL/INC
    Expected Result: PASS >= 49, no new MIXED or FAIL scripts
    Failure Indicators: PASS < 49 or new FAIL
    Evidence: .sisyphus/evidence/task-7-regression-battery.txt

  Scenario: New v1.2.1 endpoints accessible
    Tool: Bash (SSH)
    Steps:
      1. SSH to VPS
      2. Test each new RPC method via curl
      3. Assert none return "method not found"
    Expected Result: All new endpoints respond with valid JSON
    Failure Indicators: "Method not found" for any new method
    Evidence: .sisyphus/evidence/task-7-new-endpoints.txt
  ```

  **Commit**: YES
  - Message: `chore(v1.2.1): full regression battery report`
  - Files: `tests/reports/v1.2.1-regression-report.md`

- [x] 8. v1.3 roadmap document

  **What to do**:
  - Research what Browser (Jetski), Email (Postfix), and Office (sidecar) infrastructure actually require:
    - Read `jetski/` directory structure and Dockerfile
    - Read `sidecar-python/` and sidecar-rust/ directory structures
    - Assess resource requirements (RAM, disk, CPU)
  - Document what's already built vs what needs deployment vs what needs new code
  - Create phased roadmap for v1.3:
    - Phase 1: Browser (Jetski sidecar deployment + Chrome/Chromium provisioning)
    - Phase 2: Email (Postfix + SMTP + email RPC registration)
    - Phase 3: Office (Rust + Python sidecar deployment + YARA rules)
  - Include dependency analysis between phases
  - Include resource estimates for VPS (current 2GB RAM may need upgrade for browser)

  **Must NOT do**:
  - Do NOT start implementing v1.3 features
  - Do NOT deploy any new infrastructure
  - Do NOT modify any code — this is a planning document only

  **Recommended Agent Profile**:
  - **Category**: `writing`
  - **Skills**: []

  **Parallelization**:
  - **Can Run In Parallel**: NO (after T7 for accurate baseline)
  - **Parallel Group**: Wave 3
  - **Blocks**: F1-F4
  - **Blocked By**: Task 7

  **References**:
  - `jetski/` directory — Browser sidecar implementation
  - `jetski/Dockerfile` — Jetski container requirements
  - `bridge/pkg/email/` — Email package (many files, mostly complete)
  - `bridge/pkg/toolsidecar/toolsidecar.go` — Sidecar provisioning
  - `sidecar-python/` — Python MarkItDown sidecar
  - `tests/reports/pre-beato-complete-report.md` — BEATO coverage analysis
  - `.sisyphus/reports/pre-beato-coverage-baseline.md` — Detailed BEATO gaps

  **Acceptance Criteria**:
  - [ ] `docs/reference/v1.3-roadmap.md` exists with all sections
  - [ ] Browser, Email, Office sections each have: what's built, what's needed, resource estimates
  - [ ] Dependency graph between phases
  - [ ] VPS resource assessment

  **QA Scenarios**:
  ```
  Scenario: Roadmap document is complete
    Tool: Bash
    Steps:
      1. `test -f docs/reference/v1.3-roadmap.md && echo "EXISTS"`
      2. `grep -c "Browser\|Email\|Office" docs/reference/v1.3-roadmap.md`
      3. `grep -c "resource\|RAM\|deployment" docs/reference/v1.3-roadmap.md`
    Expected Result: File exists, contains Browser/Email/Office sections, includes resource estimates
    Evidence: .sisyphus/evidence/task-8-roadmap.txt
  ```

  **Commit**: YES
  - Message: `docs(v1.3): roadmap for Browser, Email, Office infrastructure deployment`
  - Files: `docs/reference/v1.3-roadmap.md`

---

## Final Verification Wave (MANDATORY — after ALL implementation tasks)

> Technical verification is fully automated/agent-executed.
> Release completion requires explicit user approval after evidence review.

- [x] F1. **Plan Compliance Audit** — `oracle`
  Read the plan. For each "Must Have": verify implementation exists. For each "Must NOT Have": search for forbidden patterns. Check evidence files. Compare deliverables against plan.
  Output: `Must Have [N/N] | Must NOT Have [N/N] | Tasks [N/N] | VERDICT: APPROVE/REJECT`

- [x] F2. **Code Quality Review** — `unspecified-high`
  Run `cd bridge && go vet ./...` + `go test ./pkg/secretary/... ./pkg/capability/... ./pkg/agent/... ./pkg/email/...` + bash syntax checks. Review for: type assertions without checks, empty catches, fmt.Println, unused imports, AI slop.
  Output: `Build [PASS/FAIL] | Tests [N/N] | VERDICT`

- [x] F3. **Live VPS Integration QA** — `unspecified-high`
  SSH to VPS (5.183.11.149). Verify artifact endpoints respond. Run regression battery. Verify delegation, sidecar, email routing, PII config endpoints.
  Output: `Scenarios [N/N] | VERDICT`

- [x] F4. **Scope Fidelity Check** — `deep`
  For each task: verify 1:1 with spec — everything built, nothing beyond spec. Check guardrails. Flag unaccounted changes.
  Output: `Tasks [N/N compliant] | VERDICT`

---

## Commit Strategy

- **Wave 0**: `feat(secretary): wire artifact RPC handlers into bridge server`
- **Wave 1**: `chore(deploy): deploy v1.2 with artifact protocol to VPS`
- **Wave 2**: `feat(secretary): v1.2.1 polish — delegation, sidecars, email routing, PII config`
- **Wave 3**: `chore(v1.2.1): regression battery + v1.3 roadmap`

---

## Success Criteria

### Verification Commands
```bash
# Artifact RPC registration
cd bridge && go test ./pkg/rpc/ -run TestArtifactMethodRegistration -v  # Expected: PASS

# All Go tests
cd bridge && go test ./pkg/secretary/... ./pkg/capability/... ./pkg/agent/... -count=1  # Expected: PASS

# VPS health
ssh -i ~/.ssh/openclaw_win root@5.183.11.149 "curl -sk https://localhost:8443/health"  # Expected: healthy

# Artifact endpoint on VPS
ssh -i ~/.ssh/openclaw_win root@5.183.11.149 "echo '{\"jsonrpc\":\"2.0\",\"id\":1,\"method\":\"secretary.artifact_list\",\"params\":{}}' | curl -sk --unix-socket /run/armorclaw/bridge.sock -X POST -H 'Content-Type: application/json' -d @- https://localhost/rpc"
```

### Final Checklist
- [ ] Artifact RPC handlers registered and responding on VPS
- [ ] Approval delegation API works end-to-end
- [ ] Dynamic sidecar spawning works with skill-based routing
- [ ] Email team-based routing dispatches correctly
- [ ] PII configuration updateable at runtime
- [ ] VPS regression: 0 new regressions
- [ ] v1.3 roadmap document exists
- [ ] All "Must NOT Have" items absent
